{
  "name": "zk-price-protection-circuits",
  "version": "1.0.0",
  "scripts": {
    "compile": "circom priceProtection.circom --r1cs --wasm --sym -o build/",
    "setup": "snarkjs groth16 setup build/priceProtection.r1cs powersOfTau28_hez_final_14.ptau priceProtection_0000.zkey",
    "contribute": "snarkjs zkey contribute priceProtection_0000.zkey priceProtection_final.zkey --name='Hackathon' --entropy='automation_seed' -v",
    "export-vkey": "snarkjs zkey export verificationkey priceProtection_final.zkey priceProtection_verification_key.json",
    "export-solidity": "snarkjs zkey export solidityverifier priceProtection_final.zkey ../contracts/src/verifier/PriceProtectionVerifier.sol",
    "build-all": "npm run compile && npm run setup && npm run contribute && npm run export-vkey && npm run export-solidity",
    "clean": "rm -rf build/* *.zkey *_verification_key.json"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  }
}